# 보안 안내

보안 문제에는 API 키 노출, 임의 파일 실행, 사용자 파일 손상과 의도하지 않은 외부 전송이 포함됨.

공개 Issue에는 실제 API 키, 사용자 이름이 포함된 전체 경로, 개인 대화, 비공개 캐릭터 이미지와 원본 로그를 올리지 않음.

보안 제보 창구가 준비되기 전에는 민감한 재현 자료를 공개 Issue에 작성하지 말고, 재현 가능 여부와 영향 범위만 최소한으로 알려주면 됨.

Lumate는 API 키를 프로젝트 파일에 저장하지 않고 Windows 환경변수에서 읽도록 설계함. 배포 전 저장 파일과 로그에 키가 남지 않는지 다시 검사함.
